<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:T11_SQLite"
             x:Class="T11_SQLite.MainPage">

    <StackLayout Margin="20,40">
        <Entry x:Name="handleEntry" Placeholder="Handle:"/>
        <Entry x:Name="existenceEntry" Placeholder="Existence:"/>
        <Button x:Name="AddPerson" Text="Add Person" Clicked="AddPerson_Clicked"/>

        <ListView x:Name="listPeople">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Handle}" Detail="{Binding Existence}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>

</ContentPage>
